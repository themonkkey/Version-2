venv/
__pycache__/
*.pyc
.env
.embed_ckpt.npy
.raw_chunks_cache.pkl
embed_index.npz
index.pkl
# Trailing slash matched only a real directory. In the Version 2 working copy
this path is a symlink to the corpus rather than a 2.8 GB duplicate, and a
symlink is not a directory to git, so the old rule let it show up as
untracked and invited committing a machine-specific absolute path.
corpus_files/vision_documents
.DS_Store

# eval/benchmark run outputs (ephemeral)
bench*_out.txt
bench_out.txt
*_pid.txt
bench_log_*.jsonl
optimize_log_*.jsonl
optimize_out.txt
rejudge_out.txt
query_emb_cache.pkl
concept_dataset.json
concept_compact.json
concept_cert.json
stress_dataset.json
rejudge_opus.json
optimized_prompt.txt
.claude/
.cohere_live_keys
streamlit_secrets.toml

# large local artifacts (rebuildable, must never be pushed)
corpus_files/gis/assembly_constituencies/
voyage_out/
pilot_chunks.pkl
pilot_index.npz
*.log
*_out.txt
haiku_tasks/
bakeoff_*.jsonl
voyage_out_512/
keys.md
KEYS.md
*.bak
*.bak.*
*.broken
.env.bak.*
app.py.bak.*
*.jsonl.bak

# scratch staging page — would be published if committed under landing/
landing/_audit_stage.html

# other session review scratch — under landing/, would publish if committed
landing/__*_tmp.html
